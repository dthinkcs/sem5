#!/bin/sh
echo "Hello i'm in bash file"
echo "Enter file name"
read name

if [ -d "${name}" ];then
echo " Entered file is a directory"
elif [ -f "${name}" ];then
echo " Entered file is a regular file"
fi  
